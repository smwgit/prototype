<h1>Edit user</h1>

<p> For now you need to enter a new (or the same) password for every edit. Sorry! </p>

<%= form_for(@user) do |f| %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<div>
  <%= gravatar_for @user %>
  <a href="http://gravatar.com/emails">change Gravatar</a>
</div>